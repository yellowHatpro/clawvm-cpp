cmake_minimum_required(VERSION 3.20)
project(clawvm)

set(CMAKE_CXX_STANDARD 20)

file(GLOB_RECURSE SOURCES
  src/*.cpp)

add_executable(clawvm
  ${SOURCES})

target_include_directories(clawvm PRIVATE include)
