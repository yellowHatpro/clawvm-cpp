load("@rules_cc//cc:cc_binary.bzl", "cc_binary")

cc_binary(
    name = "clawvm",
    srcs = glob([
        "include/**/*.hpp",
        "src/**/*.cpp",
    ]),
    copts = ["-std=c++20"],
    includes = ["include"],
    visibility = ["//visibility:public"],
)
